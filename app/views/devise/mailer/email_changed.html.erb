<p><%= t "devise.mail.hello" %> <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t "devise.mail.notify_being_change_email" %> <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p><%= t "devise.mail.notify_has_change_email" %> <%= @resource.email %>.</p>
<% end %>
